# MeloMania - История версий

## v7.5.1 (Декабрь 2024) — Автоматическое воспроизведение звуков 🎶

### Автоматизация

**Звуки воспроизводятся автоматически:**
- 🎬 Начало раунда → звук фанфар
- ▶️ Старт таймера → фоновая музыка
- ⏸ Стоп таймера → музыка затухает
- ⏱️ Последние 5 секунд → тиканье
- ✅ Показ ответа → звук правильного ответа
- ☕ Перерыв → музыка перерыва (плавно нарастает)
- 🏆 Конец игры → победный звук

### Управление в UI

**Кнопки в таймере:**
- 🎵 / 🔇 — вкл/выкл музыку таймера
- 🔔 / 🔕 — вкл/выкл звуковые эффекты (SFX)

**Окно настроек (🔊 в шапке):**
- Выбор трека для таймера/перерыва
- Регулировка громкости
- Тестирование эффектов

### Как работает
1. Положите MP3 файлы в `assets/timer-music/`
2. Запустите игру
3. Музыка включится автоматически при запуске таймера!

---

## v7.5.0 (Декабрь 2024) — Модуль системных звуков 🔊

### Новые возможности

**Модуль SystemSounds:**
- Автоматическое сканирование папок `assets/timer-music/` и `assets/break-music/`
- Поддержка файлов с русскими именами (UTF-8)
- Сгенерированные SFX звуки (тик, правильно, неправильно, старт раунда, конец игры)
- Управление громкостью по категориям

**UI Настройки звуков (кнопка 🔊 в шапке):**
- Список загруженных треков с выбором
- Предпрослушивание треков
- Регулировка громкости для музыки и эффектов
- Тестирование звуковых эффектов

**IPC для Electron:**
- `scanAudioFolder` — сканирование папок с аудио
- `getAppPath` — получение пути приложения

### Как добавить музыку:
1. Положите MP3/WAV/OGG файлы в `assets/timer-music/` для музыки вопросов
2. Положите файлы в `assets/break-music/` для музыки перерыва
3. Перезапустите приложение
4. Нажмите 🔊 в шапке для настройки

### Исправления
- TimerAudio теперь использует SystemSounds
- URL encoding для файлов с русскими именами

---

## v7.4.4 (Декабрь 2024) — Исправление предпросмотра слайда правил

### Исправления

**Предпросмотры в редакторе:**
- ❌ Модальные окна с iframe вызывали падение Electron (ошибка Autofill)
- ✅ Теперь все предпросмотры открываются в отдельных окнах через window.open
- ✅ Работают стабильно без падений

### Новые настройки

**Тень ответа (настройки проектора):**
- Цвет тени — независимо от цвета текста ответа
- Размытие (0-80px) — слайдер для точной настройки
- Кнопка "Без тени" — полностью отключает свечение

```
┌─────────────────────────────────────────────────┐
│  🌟 Тень ответа                                 │
│  [🎨 цвет] как текст   Размытие: ═══●═══ 40px   │
│                                    [Без тени]   │
└─────────────────────────────────────────────────┘
```

**По умолчанию:**
- Цвет тени = цвет текста ответа
- Размытие = 40px

**Для отключения яркой тени:**
- Уменьшите размытие до 0
- Или нажмите "Без тени"

---

## v7.4.1 (Декабрь 2024) — Исправления превью

### Исправления

**Экран ведущего:**
- ✅ Исправлена кнопка "Начать раунд" — теперь корректно переходит в фазу вопросов
- ✅ Убрана мемоизация previewData — гарантированное обновление при любом изменении
- ✅ iframe теперь заполняет всю доступную область

**Редактор:**
- ✅ Предпросмотр слайда правил теперь работает (модальное окно вместо popup)
- ✅ Добавлены две отдельные кнопки: **"👁️ Вопрос"** и **"✅ Ответ"**
- ✅ В модальном окне можно переключаться между вопросом и ответом

### Новый интерфейс предпросмотра в редакторе

```
┌─────────────────────────────────────────────┐
│  ✏️ Редактирование вопроса 1               │
│  ...поля вопроса...                         │
│  ┌──────────────────┬──────────────────────┐│
│  │  👁️ Вопрос      │    ✅ Ответ          ││
│  └──────────────────┴──────────────────────┘│
└─────────────────────────────────────────────┘
```

---

## v7.4.0 (Декабрь 2024) — Превью проектора на экране ведущего 🎉

### Главное изменение

**Превью проектора в реальном времени:**
- Теперь ведущий видит ТОЧНУЮ копию того, что показывается на проекторе
- iframe обновляется в реальном времени при каждом изменении
- Бейдж "📺 Это видит публика" для наглядности
- Можно открыть проектор в любой момент — не нужно перезапускать игру!

### Новый интерфейс ведущего

```
┌─────────────────────────────────────────────┬──────────────┐
│  🎮 Заголовок игры                          │              │
├─────────────────────────────────────────────┤   Sidebar    │
│  ┌───────────────────────────────────────┐  │   (раунды,   │
│  │  📺 ПРЕВЬЮ ПРОЕКТОРА (16:9)           │  │    вопросы,  │
│  │  [Точная копия экрана проектора]      │  │    команды)  │
│  └───────────────────────────────────────┘  │              │
│  ┌───────────────────────────────────────┐  │              │
│  │  ✅ Ответ: ... | ⏱️ Таймер | [Далее]  │  │              │
│  └───────────────────────────────────────┘  │              │
└─────────────────────────────────────────────┴──────────────┘
```

### Улучшения редактора

**Кнопка предпросмотра слайда правил:**
- Исправлена работа кнопки (добавлен fallback для popup blocker)
- Открывает отдельное окно 1280×720

**Объединение блоков настроек раунда:**
- "Настройки слайда правил" теперь секция внутри блока раунда
- Более логичная структура

---

## v7.3.4 (Декабрь 2024) — Объединение блоков редактора

### Изменения UI

**Упрощение секции "Слайд правил раунда":**
- Убран встроенный предпросмотр 16:9
- Оставлена только кнопка "👁️ Предпросмотр слайда" (открывает отдельное окно)
- Более компактный layout

**Объединение блоков:**
- Блоки "Настройки проектора" и "Редактор вопроса" объединены в один физический блок
- Визуальный разделитель между секциями
- Убрана лишняя кнопка предпросмотра из настроек проектора

**Улучшения выбора цвета:**
- Отображение выбранного HEX-кода рядом с пикером
- Кнопка сброса (✕) появляется только когда цвет установлен
- Показывает "авто" когда цвет не задан
- Убрано поле "Прозрачность" (редко использовалось)

---

## v7.3.3 (Декабрь 2024) — Кнопки предпросмотра проектора

### Новые возможности

**Предпросмотр в редакторе:**
- 👁️ **"Открыть предпросмотр на весь экран"** в секции слайда правил — открывает окно 1280×720 с точным видом слайда правил раунда
- 👁️ **"Предпросмотр на проекторе"** в настройках проектора — показывает как будет выглядеть текущий вопрос

**Улучшения UI:**
- Кастомные чекбоксы в "Настройках проектора" (как в слайде правил)
- Иконки рядом с каждым пунктом чекбокса
- Кликабельные строки (не только сам чекбокс)
- Подсветка выбранных элементов

---

## v7.3.2 (Декабрь 2024) — Улучшенный редактор слайда правил

### Изменения UI редактора

**Новый дизайн секции "Слайд правил раунда":**
- Двухколоночный layout: настройки слева, предпросмотр справа
- **Полноценный предпросмотр 16:9** — теперь видно как слайд будет выглядеть на проекторе
- **Кастомные чекбоксы** — визуально понятные, с галочкой и подсветкой
- Компактные кнопки позиции картинки (иконки вместо текста)
- Группировка элементов в логические секции
- Кликабельные строки чекбоксов (не только сам чекбокс)

**Предпросмотр показывает:**
- Реальное расположение элементов (картинка слева/справа/сверху/фон)
- Все включённые элементы (номер, название, тема, бейджи, правила)
- Акцентный цвет на бейджах и правилах
- Картинку раунда в правильном масштабе
- Режим "картинка как фон" с затемнением

---

## v7.3.1 (Декабрь 2024) — Мелкие улучшения

### Изменения

- **TimerAudio.ts**: Добавлен DEBUG режим (`TIMER_AUDIO_DEBUG`), console.log теперь выводятся только при включённой отладке
- **start.bat**: Добавлена автоматическая проверка и переустановка Electron если он не найден
- **fix-electron.bat**: Новый скрипт для ручного исправления проблем с Electron на Windows

---

## v7.3.0 (Декабрь 2024) — Профессиональный макет слайда правил

### Новый макет слайда правил раунда

**Классический квиз-формат:**
```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│  ① Раунд 1 из 8                          ┌──────────┐   │
│                                          │          │   │
│  🎵 МУЗЫКАЛЬНЫЙ РАУНД                    │  🖼️      │   │
│  Тема: Хиты 90-х                         │ КАРТИНКА │   │
│                                          │          │   │
│  ❓ 8 вопросов                           │          │   │
│  ⏱️ 60 секунд на ответ                   └──────────┘   │
│  ⭐ 2 балла за правильный ответ                         │
│                                                          │
│  📋 Прослушайте фрагмент и угадайте...                  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### Новые типы

```typescript
// Позиция картинки
type IntroImagePosition = 'right' | 'left' | 'top' | 'background' | 'hidden';

// Размер картинки
type IntroImageSize = 'small' | 'medium' | 'large';  // 30% / 40% / 50%

// Настройки макета
interface IntroSlideLayout {
  imagePosition: IntroImagePosition;
  imageSize: IntroImageSize;
  showRoundNumber: boolean;    // "Раунд 1 из 8"
  showRoundName: boolean;      // Название раунда
  showTopic: boolean;          // Тема раунда
  showQuestionCount: boolean;  // "8 вопросов"
  showTimeLimit: boolean;      // "60 секунд"
  showPoints: boolean;         // "2 балла"
  showRules: boolean;          // Правила/условия
  showRoundIcon: boolean;      // Иконка типа раунда
  accentColor?: string;        // Кастомный цвет акцента
}
```

### Редактор макета

**Новые настройки в редакторе раунда:**
- 📍 **Позиция картинки**: справа (классика) / слева / сверху / фон / скрыть
- 📐 **Размер картинки**: S (30%) / M (40%) / L (50%)
- 🎨 **Акцентный цвет** раунда
- ✅ **Чекбоксы видимости** для каждого элемента
- 👁️ **Превью макета** в реальном времени

### Визуальные улучшения

- Двухколоночный layout для горизонтальных позиций
- Адаптивная типографика с `clamp()`
- Карточки информации с акцентной полосой слева
- Режим "картинка как фон" с затемнением
- Плавные переходы и тени

---

## v7.2.0 (Декабрь 2024)

### Улучшения UX

**Компонент FilePicker:**
- Кнопка "📁 Выбрать" вместо текстовых полей для путей
- Отображение имени выбранного файла
- Кнопка удаления (✕) для сброса
- Автоматическое определение относительных путей
- Предложение установить basePath при выборе файла вне папки

**Секция "Слайд правил раунда":**
- FilePicker для картинки раунда с превью
- FilePicker для фоновой мелодии
- Ползунок громкости

### Улучшения экспорта/импорта

**Экспорт:**
- Подсчёт медиафайлов в квизе
- Предупреждение об абсолютных путях
- Инструкция по переносимости квиза
- basePath не экспортируется (для чистоты JSON)

**Импорт:**
- Валидация структуры JSON
- Проверка наличия name и rounds
- Проверка корректности раундов
- Подробная статистика после импорта
- Подсказки по медиафайлам

### Код-ревью экспорта

**Обнаруженные проблемы (исправлены):**
1. ❌ basePath экспортировался с абсолютным путём → ✅ Теперь не экспортируется
2. ❌ Нет предупреждения о медиа → ✅ Добавлено информативное сообщение
3. ❌ Слабая валидация импорта → ✅ Полная проверка структуры

**Рекомендации по переносу квизов:**
```
quiz_folder/
├── quiz.json          ← JSON файл квиза
├── images/            ← Папка с картинками
│   ├── round1.jpg
│   └── question1.png
├── audio/             ← Папка с аудио
│   └── track1.mp3
└── video/             ← Папка с видео
    └── clip1.mp4
```

При импорте на другую машину:
1. Копируем всю папку quiz_folder/
2. Импортируем quiz.json
3. basePath автоматически устанавливается на директорию JSON

---

## v7.1.0 (Декабрь 2024)

### Новые возможности

**Настройки слайда правил раунда:**
- 🖼️ Картинка раунда (`introImagePath`) - декоративное изображение на слайде правил
- 🎵 Фоновая мелодия (`introMusicPath`) с настройкой громкости (0-100%)
- ⭐ Баллы по умолчанию (`defaultPoints`) - отображается на слайде
- 📊 Детальные блоки с количеством вопросов, баллами и временем (`showRulesDetails`)

**Настройки области "Вопрос X из Y":**
- Размер текста (S/M/L) - `questionNumberFontSize`
- Цвет текста - `questionNumberColor`

**Редактор:**
- Новая секция "🎬 Слайд правил раунда" в редакторе раунда
- Настройки размера и цвета "Вопрос X из Y" в разделе проектора

### Изменения типов

```typescript
// PubQuizRound - новые поля:
introImagePath?: string;      // Картинка для слайда правил
introMusicPath?: string;      // Фоновая мелодия
introMusicVolume?: number;    // Громкость (0-100)
defaultPoints?: number;       // Баллы по умолчанию
showRulesDetails?: boolean;   // Показывать блоки деталей

// RoundDisplaySettings - новые поля:
questionNumberFontSize: FontSize;  // Размер "Вопрос X из Y"
questionNumberColor?: string;      // Цвет "Вопрос X из Y"
```

---

## v7.0.0 (Декабрь 2024) - Performance & Responsiveness Update

## Обзор изменений

Данный рефакторинг направлен на три ключевых аспекта:
1. **Производительность** - GPU-ускорение, мемоизация, оптимизация рендеринга
2. **Адаптивность** - поддержка различных размеров экранов и устройств
3. **Совместимость** - поддержка различных браузеров и устройств

---

## 1. Производительность

### 1.1 CSS-оптимизации (projector/styles.ts, global.css)

- **GPU-ускорение анимаций**: Использование `transform: translateZ(0)` и `will-change` для композитных слоёв
- **CSS Containment**: Добавлен `contain: layout style` для изоляции рендеринга
- **Fluid Typography**: Использование `clamp()` вместо фиксированных значений
- **CSS Variables**: Централизованные переменные для динамической темизации

```css
/* Пример GPU-ускоренной анимации */
.timer.warn {
  will-change: transform, color;
  transform: translateZ(0);
  animation: pulse 0.5s ease-in-out infinite;
}
```

### 1.2 React-оптимизации

- **useMemo/useCallback**: Мемоизация тяжёлых вычислений и callbacks
- **React.memo**: Мемоизация компонентов (ThemeButton, MediaPlayer)
- **Throttled updates**: Обновление времени плеера раз в 250ms вместо каждого кадра

```typescript
// Throttled time update для MediaPlayer
const throttledSetTime = useMemo(
  () => throttle((time: number) => setCurrentTime(time), 250),
  []
);
```

### 1.3 Атмосферные эффекты

- **Уменьшено количество частиц** на 50% для лучшей производительности:
  - winter: 60 → 30
  - cyber-night: 50 → 25
  - emerald-tech: 40 → 20
  - и т.д.

- **Адаптивное количество**: Хук `useOptimalAnimations()` автоматически уменьшает количество частиц на слабых устройствах

---

## 2. Адаптивность

### 2.1 Responsive Breakpoints

```typescript
export const BREAKPOINTS = {
  xs: 0,      // < 480px (мобильные)
  sm: 480,    // >= 480px
  md: 768,    // >= 768px (планшеты)
  lg: 1024,   // >= 1024px (десктоп)
  xl: 1280,   // >= 1280px
  xxl: 1536,  // >= 1536px (большие мониторы)
};
```

### 2.2 Хуки адаптивности (hooks/useResponsive.ts)

- **useResponsive()** - полное состояние адаптивности
- **useBreakpointValue()** - значения для разных breakpoints
- **useContainerSize()** - отслеживание размера контейнера
- **useOptimalAnimations()** - оптимальные анимации для устройства
- **useKeyboardNavigation()** - навигация с клавиатуры
- **useSafeArea()** - отступы для notched устройств

### 2.3 Мобильная адаптация PubQuizModule

- Sidebar переключается в горизонтальный режим на мобильных
- Адаптивные отступы и размеры шрифтов
- Touch-friendly элементы (минимум 44px)

```typescript
// Автоматическое определение layout
const isMobileLayout = responsive.deviceType === 'mobile' || responsive.width < 768;
```

### 2.4 Проектор (renderer.ts)

- **Fluid typography**: Размеры шрифтов адаптируются к размеру экрана
- **Safe area insets**: Поддержка notched устройств
- **Responsive media**: Изображения и видео масштабируются корректно

```css
.qtxt {
  font-size: clamp(32px, 5vw, 64px);
  max-width: min(1400px, 90vw);
}
```

---

## 3. Совместимость

### 3.1 Браузерная совместимость

- **Webkit prefixes**: `-webkit-backdrop-filter`, `-webkit-font-smoothing`
- **Fallbacks**: Для устройств без backdrop-filter
- **Legacy event listeners**: Поддержка старых браузеров

```css
.glass {
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
}

@supports not (backdrop-filter: blur(20px)) {
  .glass {
    background-color: rgba(0, 0, 0, 0.8) !important;
  }
}
```

### 3.2 Accessibility

- **prefers-reduced-motion**: Отключение анимаций
- **prefers-color-scheme**: Автоопределение темы
- **prefers-contrast**: Высококонтрастный режим
- **ARIA labels**: Для медиаплеера
- **Focus visible**: Стили для навигации с клавиатуры

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
  .atmosphere { display: none !important; }
}
```

### 3.3 Touch устройства

- Минимальный размер touch-target: 44px
- `-webkit-tap-highlight-color: transparent`
- `touch-action: manipulation` для кнопок
- `playsInline` для видео на iOS

### 3.4 Print стили

```css
@media print {
  .atmosphere { display: none !important; }
  body { background: white !important; color: black !important; }
}
```

---

## 4. Новые утилиты (utils/performance.ts)

```typescript
// Debounce для resize events
debounce(fn, wait)

// Throttle для частых обновлений
throttle(fn, limit)

// RAF-based throttle для анимаций
rafThrottle(fn)

// Определение возможностей устройства
getDeviceCapabilities()

// Оптимальное количество частиц
getOptimalParticleCount(baseCount)

// Мониторинг FPS
new PerformanceMonitor()

// Batch DOM updates
domBatcher.add(update)

// Safe localStorage
safeStorage.get/set/remove
```

---

## 5. Структура файлов

```
src/renderer/
├── components/pubquiz/
│   ├── projector/
│   │   ├── renderer.ts      # Обновлён с адаптивными стилями
│   │   └── styles.ts        # Полностью переписан с CSS variables
│   ├── ui/
│   │   └── MediaPlayer.tsx  # Оптимизирован с throttle
│   └── PubQuizModule.tsx    # Добавлена мобильная адаптация
├── hooks/
│   └── useResponsive.ts     # Расширен новыми хуками
├── styles/
│   └── global.css           # Переписан с CSS variables
└── utils/
    ├── index.ts
    └── performance.ts       # Новый файл утилит
```

---

## 6. Рекомендации по использованию

### Для слабых устройств

```typescript
const { shouldAnimate, particleCount } = useOptimalAnimations();

// Используйте условный рендеринг
{shouldAnimate && <AtmosphereEffects count={particleCount(30)} />}
```

### Для мобильных устройств

```typescript
const { isTouchDevice, deviceType } = useResponsive();

// Адаптируйте UI
const buttonSize = deviceType === 'mobile' ? 'large' : 'medium';
```

### Для клавиатурной навигации

```typescript
useKeyboardNavigation({
  onNext: () => nextQuestion(),
  onPrev: () => prevQuestion(),
  onSpace: () => toggleTimer(),
});
```

---

## 7. Тестирование

Рекомендуется протестировать на:
- [ ] Desktop Chrome/Firefox/Safari
- [ ] iOS Safari (iPhone, iPad)
- [ ] Android Chrome
- [ ] Устройства с reduced-motion
- [ ] Низкопроизводительные устройства
- [ ] Большие мониторы (4K)
- [ ] Проекторы

---

## Версия

**MeloMania v7.0** - Performance & Responsiveness Update
Дата: Декабрь 2024
